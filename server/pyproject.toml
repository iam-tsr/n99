[project]
name = "n99-server"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "selenium",
    "beautifulsoup4",
    "loguru",
    "fastapi",
    "pydantic",
    "uvicorn",
    "lxml",
    "apscheduler",
    "playwright",
]
